import React from "react";

const topButton = ({text, borderColor, textColor, backgroundColor, icon}) => {
    return(
        <button className="PUTSOMENAMEINHERE">
        <img src={icon} height={10} width={10}/>
        <p className ="" >{text}</p>
            
        
        </button>


    );

} 